# Hybrid RAG Orchestrator - Dependencies
# Python 3.12.11 기준

# ========================================================================
# 1. 웹 프레임워크 (FastAPI)
# ========================================================================
fastapi==0.116.1
uvicorn[standard]==0.32.1
python-multipart==0.0.19
jinja2==3.1.5

# ========================================================================
# 2. LangChain 생태계 (LLM 오케스트레이션)
# ========================================================================
langchain==0.3.27
langchain-core==0.3.74
langchain-community==0.3.27
langchain-ollama==0.3.6
langchain-google-community==2.0.7
langchain-text-splitters==0.3.9

# ========================================================================
# 3. LangGraph (StateGraph 기반 워크플로우)
# ========================================================================
langgraph

# ========================================================================
# 4. 벡터 데이터베이스 및 임베딩
# ========================================================================
chromadb==1.0.17
sentence-transformers==5.1.0
torch==2.8.0
transformers==4.55.2
numpy==2.1.0
tf-keras==2.20.1

# ========================================================================
# 5. PDF 처리
# ========================================================================
PyMuPDF==1.26.3

# ========================================================================
# 6. 네트워크 통신
# ========================================================================
websockets==15.0.1
httpx==0.28.1

# ========================================================================
# 7. 데이터 검증 및 설정
# ========================================================================
pydantic==2.11.7
pydantic-settings==2.10.1
python-dotenv==1.0.1

# ========================================================================
# 8. 로깅 및 진행 표시
# ========================================================================
loguru==0.7.3
tqdm==4.67.1
psutil==7.1.2

# ========================================================================
# 9. 개발 도구 (선택사항 - 주석 처리)
# ========================================================================
# pytest==8.3.5
# pytest-asyncio==0.25.2
# black==24.10.0
# ruff==0.8.6

# ========================================================================
# 설치 방법
# ========================================================================
# pip install -r requirements.txt

# ========================================================================
# 주요 패키지 용도
# ========================================================================
# - FastAPI: RESTful API 서버 (비동기 지원)
# - LangChain: LLM 체인 및 프롬프트 관리
# - LangGraph: StateGraph 기반 워크플로우 오케스트레이션
# - ChromaDB: 벡터 저장소 (코사인 유사도 검색)
# - Sentence-Transformers: 한국어 임베딩 모델 (jhgan/ko-sbert-nli)
# - PyMuPDF: PDF 텍스트 추출 및 청킹
# - WebSockets: MCP 서버 JSON-RPC 통신
# - HTTPX: 비동기 HTTP 클라이언트 (Google Search API)
# - Loguru: 구조화된 로깅 (색상, 레벨별 필터링)
